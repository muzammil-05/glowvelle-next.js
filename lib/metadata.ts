/** @deprecated Import from `@/lib/seo` instead. Kept for backward compatibility. */
export {
  createBlogPostMetadata,
  createBrandMetadata,
  createCategoryMetadata,
  createPageMetadata,
  createProductMetadata,
  getDefaultOgImageUrl,
  resolveAbsoluteImageUrl,
} from "@/lib/seo/metadata";
