import {
  DEFAULT_OG_IMAGE,
  SITE_DESCRIPTION,
  SITE_NAME,
  SITE_URL,
  TWITTER_HANDLE,
} from "@/config/site";

export {
  SITE_DESCRIPTION,
  SITE_NAME,
  SITE_URL,
  TWITTER_HANDLE,
};

/** Hero Unsplash photo id used when no dedicated OG asset exists. */
export const DEFAULT_OG_UNSPLASH_ID = "photo-1779056904689-ff99fd0045a4";

/** Relative path for default OG image in /public (fallback to Unsplash if missing). */
export const DEFAULT_OG_IMAGE_PATH = DEFAULT_OG_IMAGE;

export { CONTACT } from "@/config/site";

export const SEARCH_PATH = "/search";
